main {
  writeln# f(5);
  writeln# f(105);
}

f(x) {
  if (x == 1) { return x; }
  else if (x%2 == 0) { return g(x); }
  else { return h(x); }
}

g(x) {
  return f(x/2);
}

h(x) {
  return f(3*x+1);
}
